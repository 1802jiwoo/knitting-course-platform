# LoopLearn Project

## Language Rule

* 모든 설명은 한국어
* 코드명은 변경하지 않음

---

## Project Overview

Frontend: Flutter (Clean Architecture + Provider)
Backend: Spring Boot + MySQL

---

## 로그 파일 위치

* 버그 기록: /docs/p2/bugfix_log.md
* 상태 관리: /docs/p2/plans/project_status.md
* 세션 임시 저장: /docs/p2/session_checkpoint.md (임시)

---

## Development Phase

P1: 학습 기능
P2: 인증/서버 연동
P3: 확장/최적화

---

## Domain Rules

* Lecture / Part / Pattern 중심 구조
* Tag 기반 분류 시스템

---

## Project Status Rule (핵심)

* 모든 기능 변경 시 Project Status 업데이트 필수
* 상태: [✓], [⁓], [ ]
* 기능 추가 시 반드시 Status에 반영

---

## Backend 작업 프로세스 규칙

백엔드 작업은 반드시 아래 순서를 따른다:

1. 개발 완료 후 1차 코드 리뷰 보고

    * 구현 내용 요약
    * 변경된 API/로직 설명

2. 테스트 수행 전 필수 확인 단계

    * application.properties (또는 application.yml) 확인
    * 현재 active server URL 확인
    * 실제 실행 중인 서버 주소를 사용자에게 먼저 보고

3. 사용자 OK 이후 API 테스트 수행

    * Postman 또는 테스트 코드 기준

4. 테스트 결과 2차 보고

    * 요청/응답 결과 포함
    * 성공/실패 여부 명시

5. 사용자 최종 OK 이후에만 적용 완료 처리

---

## Bug Fix Log 규칙

개발 도중 문제가 발생하면 반드시 bugfix_log.md에 기록한다.

작성 형식:

* 증상: 어떤 문제가 발생했는지
* 원인: 왜 발생했는지 분석
* 수정 내용: 어떻게 해결했는지

규칙:

* 모든 버그는 반드시 기록해야 한다
* 단순 실수도 기록 대상이다
* 해결 후에도 로그는 삭제하지 않는다

---

## Session Checkpoint / Safety Rule (핵심 추가)

### 🚨 자동 저장 트리거 조건

다음 조건 중 하나라도 발생하면 반드시 작업을 중단하고 session_checkpoint.md에 현재 상태를 저장한다:

* token usage 80% 이상
* cost usage 80% 이상
* message limit 90% 이상
* session reset / timeout 1시간 이내
* limit / cost / token / budget 관련 경고 발생
* 추가 진행 시 안정성이 낮다고 판단되는 경우

---

### 저장 파일 규칙

* 저장 파일: /docs/p2/session_checkpoint.md
* 해당 파일은 **임시 파일(temporary)** 로만 사용한다
* 동일 세션 내 진행 상황 보존 목적

---

### 저장 규칙

* 현재까지 “확인된 사실만” 기록
* 추측은 Hypothesis로 분리
* 다음 실행 가능한 단계까지 포함
* 추가 디버깅/추론 금지 (저장만 수행)

---

### 세션 종료 및 삭제 규칙 (중요)

* 문제가 해결되거나 작업이 완료되면 session_checkpoint.md 내용은 반드시 삭제한다
* 삭제 기준:

    * 사용자에게 해결 완료 승인(OK)을 받은 경우
    * 해당 이슈가 bugfix_log.md로 이전 완료된 경우

---

### 최종 정리 흐름

1. 위험 감지 시 session_checkpoint.md 저장
2. 작업 중단
3. 세션 재시작 시 해당 파일로 복구
4. 해결 완료 시 bugfix_log.md로 이관
5. session_checkpoint.md 내용 삭제

---

## 작업 실행 규칙

사용자 명령:

* claude-code run front
* claude-code run backend
* claude-code run next

---

## 기본 동작

모든 작업은 ProjectStatus.md 기준으로 선택한다.

선택 우선순위:

1. [⁓] 진행 중 작업
2. [ ] 미시작 작업

각 실행마다 "1개 작업만" 수행한다.

---

## front / backend 필터

* front: 프론트 관련 작업만
* backend: 백엔드 관련 작업만

---

## next

전체에서 가장 우선순위 높은 작업 1개 선택

---

## next 명령 규칙

* ProjectStatus.md에서 다음 작업 1개를 "선정만" 한다
* 절대 자동으로 작업을 시작하지 않는다
* 선정 결과를 사용자에게 먼저 보고한다
* 사용자의 승인 이후에만 실행한다

## front / backend / next 공통 규칙

* 작업 시작 전 반드시 "선택된 작업"을 먼저 사용자에게 보고한다
* 사용자가 OK를 하기 전까지 코드 작성 금지

---

## 우선순위 기준

1. 의존성 있는 작업
2. 핵심 기능
3. 영향도 높은 기능
4. 기타

---

## 멀티 작업 규칙

기본 1개 작업만 수행

단, 해당 작업에 반드시 필요한 의존 작업이 있을 경우만 함께 수행 가능

---

## 필수 규칙

* 실행 전 ProjectStatus.md 반드시 확인
* 작업 후 상태 반드시 업데이트
